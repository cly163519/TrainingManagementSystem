<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.laura.tmsbackend.mapper.TeachingAssignmentMapper">
    <insert id="insert">
        INSERT INTO teaching_assignment(training_class_id, trainer_id)
        VALUES(#{trainingClassId}, #{trainerId})
    </insert>

    <delete id="deleteById">
        DELETE FROM teaching_assignment
        WHERE id = #{id}
    </delete>

    <update id="update">
        UPDATE teaching_assignment SET
                                       training_class_id = #{trainingClassId},
                                       trainer_id = #{trainerId}
        WHERE id = #{id}
    </update>

    <select id="findById" resultType="com.laura.tmsbackend.model.TeachingAssignment">
        SELECT id, training_class_id, trainer_id
        FROM teaching_assignment WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.laura.tmsbackend.model.TeachingAssignment">
        SELECT id, training_class_id, trainer_id
        FROM teaching_assignment
    </select>

</mapper>