<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.laura.tmsbackend.mapper.UserMapper">

    <select id="findByUsername" resultType="com.laura.tmsbackend.model.User">
        SELECT id, username, password, role, real_name
        FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.laura.tmsbackend.model.User">
        SELECT id, username, password, role, real_name
        FROM user WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.laura.tmsbackend.model.User">
        SELECT id, username, password, role, real_name
        FROM user
    </select>

    <insert id="insert">
        INSERT INTO user(username, password, role, real_name)
        VALUES(#{username}, #{password}, #{role}, #{realName})
    </insert>

    <update id="update">
        UPDATE user SET
                        username = #{username},
                        password = #{password},
                        role = #{role},
                        real_name = #{realName}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>